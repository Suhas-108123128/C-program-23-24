#include <stdio.h>
int main()
{
	float a,b,c;
	int d;
	printf("Enter the two numbers:");
	scanf("%f %f",&a,&b);
	c=a*b;
	d=a*b;
	printf("The floating product of two numbers is %f:",c);
	printf("The integer product of two numbers is %d :",d);
	return 0;
}
